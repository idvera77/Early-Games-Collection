<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <stroke android:width="2dp" android:color="@color/borderEvento" />
            <gradient
                android:startColor="@color/barraSaludVacia"
                android:centerColor="@color/barraSaludVacia"
                android:centerY="1.0"
                android:endColor="@color/barraSaludVacia"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <stroke android:width="2dp" android:color="@color/borderEvento" />
                <gradient
                    android:startColor="@color/barraSaludLlena"
                    android:centerColor="@color/barraSaludLlena"
                    android:centerY="1.0"
                    android:endColor="@color/barraSaludLlena"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>